import transformers
import cv2
import os

print(f"Transformers version: {transformers.__version__}")
print(f"OpenCV (cv2) version: {cv2.__version__}")
print(f"Python (os) version: {os.sys.version}")  # os is built-in, shows Python version